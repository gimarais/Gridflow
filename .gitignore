node_modules/
dist/
out/
out-webview/
*.vsix
.DS_Store
*.log
.vscode-test/

# Sidecars written by integration tests into the test workspace
test-workspace/.gridflow/

# CLI tool — kept LOCAL only, future premium feature. The MCP server (mcp/)
# is public; the CLI UI (cli/) is gated.
cli/**

# Demo GIF recorder tooling — kept LOCAL only, never published to the public repo.
# The generated GIFs in media/ ARE committed; only the recorder source is ignored.
scripts/demo/

# Local / machine-specific dev config — never commit (contains absolute paths)
.mcp.json
.vscode/settings.json
.claude/settings.local.json
